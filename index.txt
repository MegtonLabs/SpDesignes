<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blurred Gradient Border (Border-Image)</title>
    <style>
      body {
        margin: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: url("./zz.jpeg") no-repeat center center/cover;
        position: relative;
      }

      body::before {
        content: "";
        position: absolute;
        inset: 0;
        background: url("./zz.jpeg") no-repeat center center/cover;
        filter: blur(12px);
        /* transform: scale(1.1); */
      }

      .card {
        width: 300px;
        height: 350px;
        border-radius: 20px;
        background: linear-gradient(
          to bottom,
          rgba(39, 109, 167, 0) 0%,
          rgba(39, 109, 167, 0.2) 70%,
          rgba(39, 109, 167, 0.531) 100%
        );
        position: relative;
        overflow: hidden;
        /* border:1px solid rgba(198, 198, 198, 0.107); */
        box-shadow: inset 0 8px 22px 0 rgba(0, 0, 0, 0.15);
      }

      .card::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: 20px;
        border: 20px solid transparent;
        border-image: linear-gradient(
            to bottom,
            rgba(247, 244, 244, 0) 0%,
            rgba(46, 46, 46, 0.4) 50%,
            rgb(253, 254, 255) 100%
          )
          1;
        filter: blur(10px);
        pointer-events: none;
        z-index: 2;
        mix-blend-mode: overlay;
      }

      /* Top border glow */
      .card::after {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;

        border-radius: 20px;
        padding: 2px;
        background: conic-gradient(
          from 290deg,
          #0b317e48,
          #41d5ff 8%,
          #1f4eab0c 14%,
          transparent 70%
        );

        -webkit-mask: linear-gradient(#fff 0 0) content-box,
          linear-gradient(#fff 0 0);
        mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;

        /* opacity:70%; */
      }

      

      /* Bottom border glow */
      .card .BB-glow {
        position: absolute;
        inset: 0;
        border-radius: 20px;
        padding: 2px;
        background: conic-gradient(
          from 280deg,
          /* flipped to target bottom */ transparent 40%,
          #1f4eab0c 40%,
          #41d5ff 60%,
          #0b317e48 100%
        );
        filter: blur(5px);

        -webkit-mask: linear-gradient(#fff 0 0) content-box,
          linear-gradient(#fff 0 0);
        mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;

        pointer-events: none;
      }

    
      .card > * {
        position: relative;
        z-index: 3;
      }
    .bottom-glow {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 120px;
  background: url("./Lights/bottom-light/Light1.svg") no-repeat center bottom;
  background-size: contain; /* preserves proportions */
  pointer-events: none;
  z-index:3;
  mix-blend-mode: overlay;
  filter: blur(20px);
}

      .top-shin {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100px; /* adjust based on SVG size */
  background: url("./Lights/top-light/card-top-shine.svg") no-repeat center top;
  background-size: cover; /* or contain if you want exact fit */
  pointer-events: none; /* don’t block clicks */
  z-index: 2; /* below text but above background */
  mix-blend-mode: overlay;
  filter: blur(10px);
}





    </style>
  </head>
  <body>
    <div class="card">
      <div class="BB-glow"></div>
      <div class="top-shin"></div>
      <div class="bottom-glow"></div>
    </div>
  </body>
</html>
